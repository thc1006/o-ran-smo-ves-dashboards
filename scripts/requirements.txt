# Dev / probe dependencies (NOT runtime deps of the dashboards themselves,
# which are pure JSON).
#
# pytest-ves 0.2.1 landed on PyPI on 2026-04-19
# (https://pypi.org/project/pytest-ves/0.2.1/). Install normally:
#
#     pip install -r scripts/requirements.txt
#
# For hacking on pytest-ves in parallel, prefer an editable install
# from a local clone instead:
#
#     pip install -e ../../pytest-ves   # alongside this repo

pytest-ves>=0.2.1,<1
influxdb-client>=1.48
requests>=2.32
