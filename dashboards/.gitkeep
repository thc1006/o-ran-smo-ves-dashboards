Dashboards are deliberately empty in v0.1.0; see README.md "Status" and
SDD-001 §9.1 for why (Phase 1 schema probe is a prerequisite). Remove this
file and replace with .json dashboard artefacts once the probe completes.
